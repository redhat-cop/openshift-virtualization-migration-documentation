site:
  title: Ansible for OpenShift Virtualization Migration Documentation
  start_page: guides::home.adoc
content:
  sources:
    - url: .
      start_path: guides
      branches: HEAD

asciidoc:
  attributes:
    experimental: ''
    idprefix: ''
    idseparator: '-'
    page-pagination: ''

ui:
  bundle:
    # NOTE: This URL points to the default Antora community UI.
    # Tracked in issue #6: this should be replaced with the Red Hat branded
    # template (see issue #2) once that work is complete.
    # Pinned to a stable release to prevent silent breakage from upstream UI changes.
    url: https://gitlab.com/antora/antora-ui-default/-/jobs/artifacts/HEAD/raw/build/ui-bundle.zip?job=bundle-stable
    snapshot: false